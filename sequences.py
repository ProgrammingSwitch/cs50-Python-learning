#sequence used with just a string
name = "Harry"

print(name[0])

#sequence used with a list
name = ["Harry", "Herminone", "Ron"]

print(name[1])

#sequence used with a tuple *tuple is similar to a list (mutable) but is used for info that won't change (immutable) and has multilple values and identifies a single point *() is  used instead of the []
cordinate = (10.0, 20.0)

print(cordinate)